---
import type { ResumeData } from "@/data/resume-data";
import NameAndTitle from "./NameAndTitle.astro";
import ContactDetails from "./ContactDetails.astro";

type Props = {
    data: ResumeData;
};

const { data } = Astro.props as Props;
---

<header>
    <NameAndTitle {data} />
    <ContactDetails {data} />
</header>
